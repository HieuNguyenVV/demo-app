{
  "type": "object",
  "additionalProperties": false,
  "required": ["fileName", "prompt"],
  "properties": {
    "fileName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "pattern": "^[a-zA-Z0-9._-]+$",
      "description": "Base file name without extension, for example login-sequence."
    },
    "title": {
      "type": "string",
      "maxLength": 80,
      "description": "Optional diagram heading. Vietnamese is fine."
    },
    "prompt": {
      "type": "string",
      "minLength": 8,
      "maxLength": 2000,
      "description": "The user's sequence-diagram request in their own words, including if/else or loops. Do not invent participants or messages; OpenAI will draw them."
    }
  }
}
