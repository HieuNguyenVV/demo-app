{
  "type": "object",
  "additionalProperties": false,
  "required": ["fileName", "prompt"],
  "properties": {
    "fileName": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "pattern": "^[a-zA-Z0-9._-]+$",
      "description": "Base file name without extension, for example checkout-flow."
    },
    "title": {
      "type": "string",
      "maxLength": 80,
      "description": "Optional diagram heading. Vietnamese is fine."
    },
    "prompt": {
      "type": "string",
      "minLength": 8,
      "maxLength": 2000,
      "description": "The user's diagram request in their own words. Do not invent nodes or edges; OpenAI will draw the flowchart."
    }
  }
}
