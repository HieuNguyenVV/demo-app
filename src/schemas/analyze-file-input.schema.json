{
  "type": "object",
  "additionalProperties": false,
  "required": ["fileId"],
  "properties": {
    "fileId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 80,
      "description": "Identifier returned by upload-file for this workspace."
    },
    "topWordsLimit": {
      "type": "integer",
      "minimum": 3,
      "maximum": 10,
      "description": "How many frequent words to return for the UI card. Defaults to 5."
    }
  }
}
