{
  "type": "object",
  "additionalProperties": false,
  "required": ["query"],
  "properties": {
    "query": {
      "type": "string",
      "minLength": 2,
      "maxLength": 400,
      "description": "The user's search question, in their own words. Do not rewrite it; the backend will."
    }
  }
}
