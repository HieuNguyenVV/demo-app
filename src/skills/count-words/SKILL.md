---
name: count-words
description: Use when the user needs an exact word, character, or line count from count-words.
---

# Count words

Call `count-words` with the full passage in `text`. Do not summarize or rewrite the text first.

Report the returned `wordCount`, `characterCount`, and `lineCount` as-is. Do not estimate counts yourself when this tool is available.
